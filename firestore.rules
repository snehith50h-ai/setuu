rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Basic rules that allow all reads and writes for now since this is a public dashboard without auth
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
